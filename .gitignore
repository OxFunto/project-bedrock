# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl
terraform/terraform.tfvars

# Lambda zip already committed so exclude future rebuilds
lambda/*.zip

# Misc
.DS_Store
*.log
